# ============================================================
# DocFlow AI — Backend Dependencies
# ============================================================

# --- Framework web ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9          # Upload fichiers multipart

# --- Base de données ---
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0                  # Driver PostgreSQL async
alembic==1.13.1                  # Migrations DB
pgvector==0.3.2                  # Support vecteurs pgvector

# --- Validation & configuration ---
pydantic==2.7.1
pydantic-settings==2.3.0         # Config depuis .env

# --- Clients HTTP async ---
httpx==0.27.0                    # Appels Tika + Ollama

# --- Logging structuré ---
structlog==24.2.0
python-json-logger==2.0.7

# --- Traitement de documents ---
python-docx==1.1.2               # Lecture/écriture DOCX
docxtpl==0.17.0                  # Remplissage templates DOCX (Jinja2)
weasyprint==62.3                 # Export PDF depuis HTML/Markdown
markdown==3.6                    # Conversion Markdown → HTML

# --- Utilitaires ---
python-magic==0.4.27             # Détection type MIME
aiofiles==23.2.1                 # I/O fichiers async
tenacity==8.3.0                  # Retry avec backoff
python-dateutil==2.9.0

# --- Tests ---
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-timeout==2.4.0
httpx                            # Client de test FastAPI
aiosqlite==0.20.0                # Driver SQLite async pour les tests

# --- Dev ---
ruff==0.4.4                      # Linter + formatter
mypy==2.1.0                     # Type checking
