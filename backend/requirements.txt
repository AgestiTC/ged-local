# ============================================================
# DocFlow AI — Backend Dependencies
# ============================================================

# --- Framework web ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32          # Upload fichiers multipart

# --- Base de données ---
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0                  # Driver PostgreSQL async
alembic==1.19.1                  # Migrations DB
pgvector==0.5.0                  # Support vecteurs pgvector

# --- Validation & configuration ---
pydantic==2.13.4
pydantic-settings==2.15.0         # Config depuis .env

# --- Clients HTTP async ---
httpx==0.27.0                    # Appels Tika + Ollama

# --- Logging structuré ---
structlog==24.2.0
python-json-logger==2.0.7

# --- Traitement de documents ---
python-docx==1.2.0               # Lecture/écriture DOCX
docxtpl==0.20.2                  # Remplissage templates DOCX (Jinja2)
python-pptx==0.6.23              # Génération PPTX (présentations / diaporamas)
weasyprint==62.3                 # Export PDF depuis HTML/Markdown
pydyf==0.12.1                    # 🔴 ÉPINGLÉ : pydyf 0.11+ casse WeasyPrint 62.3 ("'super' object has no attribute 'transform'")
markdown==3.10.3                    # Conversion Markdown → HTML
pymupdf==1.28.2                  # Rastérisation PDF → image (OCR des PDF scannés via vision)

# --- Utilitaires ---
python-magic==0.4.27             # Détection type MIME
aiofiles==23.2.1                 # I/O fichiers async
tenacity==8.3.0                  # Retry avec backoff
python-dateutil==2.9.0

# --- Sécurité / sources réseau ---
cryptography==42.0.8             # Fernet : chiffrement des identifiants en base
pysmb==1.2.15                    # Client SMB : lister partages + parcourir + lire (sources NAS)
clamd==1.0.2                     # Client ClamAV (scan antivirus des fichiers à l'indexation)

# --- Tests ---
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-timeout==2.4.0
httpx                            # Client de test FastAPI
aiosqlite==0.22.1                # Driver SQLite async pour les tests

# --- Dev ---
ruff==0.16.5                      # Linter + formatter
mypy==1.10.0                     # Type checking
