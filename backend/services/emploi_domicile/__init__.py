"""
Module « emploi à domicile » — assistante maternelle, garde à domicile, aide ménagère.

Phase 1 : contenu en lecture seule (fiches + checklist), servi par
`routers/emploi_domicile`. Aucune table, aucune écriture.

Le nom du paquet est `emploi_domicile` et non `nounou` : « Nounou » n'est que le libellé de
son premier profil. Voir `profils.py` pour la raison — elle décide de l'architecture.
"""
